# Gemini Live Interpreter v2 - AS-IS vs TO-BE 비교

## 📋 목차
1. [개요](#개요)
2. [AS-IS: 현재 구현](#as-is-현재-구현)
3. [TO-BE: 원래 계획](#to-be-원래-계획)
4. [비교 분석](#비교-분석)
5. [의사결정 배경](#의사결정-배경)
6. [향후 고려사항](#향후-고려사항)

---

## 개요

**목적**: 백엔드 분리 아키텍처로 API 키 보안 강화 및 확장성 확보

**핵심 질문**:
- ❓ Rust + Whisper STT가 왜 적용되지 않았는가?
- ❓ Gemini Native Audio API만으로 충분한가?
- ❓ 원래 계획(project-analysis_v2.md)과 실제 구현의 차이는?

---

## AS-IS: 현재 구현

### 아키텍처

```
┌─────────────────────────────────────────┐
│   브라우저 (React - Netlify)             │
│  ┌────────────────────────────────────┐ │
│  │ • 마이크 입력 (Web Audio API)      │ │
│  │ • WebSocket 클라이언트             │ │
│  │ • 오디오 재생                      │ │
│  │ • UI/UX                            │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
            ↕ WebSocket (실시간 양방향)
┌─────────────────────────────────────────┐
│  백엔드 서버 (Python - Render.com)       │
│  ┌────────────────────────────────────┐ │
│  │ FastAPI + WebSocket                │ │
│  │ ┌────────────────────────────────┐ │ │
│  │ │  Gemini Native Audio API       │ │ │
│  │ │  (STT + 번역 + TTS + 전사)     │ │ │
│  │ └────────────────────────────────┘ │ │
│  │ • API 키 관리                      │ │
│  │ • VAD (Voice Activity Detection)  │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 기술 스택

#### 백엔드
```python
# requirements.txt
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
google-genai>=1.55.0          # Gemini SDK
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
certifi>=2024.2.2
```

#### 프로젝트 구조
```
backend/
├── app/
│   ├── main.py                    # FastAPI 진입점
│   ├── websocket/
│   │   ├── handler.py             # WebSocket 핸들러
│   │   └── protocol.py            # 메시지 프로토콜
│   ├── services/
│   │   └── gemini_service.py      # Gemini API 통합 (핵심)
│   ├── models/
│   │   └── messages.py            # Pydantic 모델
│   └── utils/
│       └── logging.py
├── rust_stt/                      # ❌ 비어있음
├── requirements.txt
└── README.md
```

### 데이터 흐름

```
1. 사용자 음성 입력
   ↓
2. 브라우저: Web Audio API로 PCM 추출
   ↓
3. 브라우저 → 백엔드: WebSocket으로 오디오 청크 전송
   ↓
4. 백엔드 → Gemini: PCM 오디오 전송
   ↓
5. Gemini 내부 처리:
   ├─ ① STT (Speech-to-Text): "안녕하세요"
   ├─ ② 번역 (LLM): "Hello"
   ├─ ③ TTS (Text-to-Speech): PCM 생성
   └─ ④ 전사: 입력/출력 텍스트 추출
   ↓
6. 백엔드 → 브라우저: WebSocket으로 결과 전송
   ├─ input_transcription: "안녕하세요"
   ├─ output_transcription: "Hello"
   └─ audio_response: PCM 오디오
   ↓
7. 브라우저: 오디오 재생 + UI 업데이트
```

### 핵심 코드

#### GeminiService 설정 (app/services/gemini_service.py)
```python
config = {
    "generation_config": {
        "response_modalities": ["AUDIO"],        # ③ TTS 응답
        "speech_config": {
            "voice_config": {
                "prebuilt_voice_config": {
                    "voice_name": "Zephyr"       # 음성 선택
                }
            }
        },
    },
    "system_instruction": SYSTEM_INSTRUCTION,   # ② 번역 지시
    "input_audio_transcription": {},            # ① 입력 STT
    "output_audio_transcription": {},           # ④ 출력 전사
}

self.session = await self.client.aio.live.connect(
    model=MODEL_NAME,  # "gemini-2.5-flash-native-audio-preview-09-2025"
    config=config
)
```

#### 메시지 처리 (app/services/gemini_service.py)
```python
async def _handle_message(self, message: LiveServerMessage):
    # ① 입력 전사 (STT 결과)
    input_trans = message.server_content.input_transcription
    if input_trans:
        self.current_input_text += input_trans.text
        await self.on_input_transcription(self.current_input_text, False)

    # ② 출력 전사 (번역 결과 텍스트)
    output_trans = message.server_content.output_transcription
    if output_trans:
        self.current_output_text += output_trans.text
        await self.on_output_transcription(self.current_output_text, False)

    # ③ 오디오 응답 (TTS 결과)
    model_turn = message.server_content.model_turn
    if model_turn:
        for part in model_turn.parts:
            audio_data = part.inline_data.data  # PCM 오디오
            encoded = base64.b64encode(audio_data).decode("ascii")
            await self.on_audio_response(encoded, 24000)

    # ④ 턴 완료
    if message.server_content.turn_complete:
        await self.on_turn_complete(
            self.current_input_text,
            self.current_output_text
        )
```

### 주요 특징

| 특징 | 설명 |
|------|------|
| **단일 API 호출** | Gemini Live API 한 번 연결로 모든 처리 |
| **실시간 스트리밍** | WebSocket으로 양방향 실시간 통신 |
| **통합 처리** | STT + 번역 + TTS + 전사를 Gemini가 통합 처리 |
| **Python Only** | 단일 언어 스택 (Rust 없음) |
| **간단한 배포** | Python 환경만 필요 (Render.com) |
| **VAD 추가** | 클라이언트 측 음성 감지로 네트워크 절약 |

### 장점 ✅

1. **구현 단순성**
   - Python만 사용 (Rust 빌드 불필요)
   - 단일 API 연결로 모든 기능 처리
   - 코드베이스 간결

2. **개발 속도**
   - 빠른 프로토타이핑
   - 복잡한 언어 간 바인딩(PyO3) 불필요
   - 디버깅 용이

3. **배포 편의성**
   - Python 환경만 필요
   - Render.com에서 즉시 배포 가능
   - Rust 빌드 파이프라인 불필요

4. **유지보수성**
   - 단일 언어 스택
   - 의존성 최소화
   - 업그레이드 간편

5. **성능**
   - 단일 API 호출로 지연 시간 최소화
   - Gemini 최적화된 STT/TTS
   - 실시간 스트리밍 지원

6. **기능 완전성**
   - 다국어 지원 (Gemini 지원 언어 전체)
   - 고품질 TTS 음성
   - 실시간 전사 제공

### 단점 ❌

1. **Gemini API 의존성**
   - 단일 벤더 종속
   - API 장애 시 서비스 중단
   - API 정책 변경 위험

2. **비용**
   - 무료 할당량 제한
   - 트래픽 증가 시 비용 발생
   - 사용량 예측 어려움

3. **커스터마이징 제약**
   - STT 모델 변경 불가
   - TTS 음성 제한적
   - 처리 로직 제어 불가

4. **오프라인 불가능**
   - 인터넷 연결 필수
   - 로컬 처리 불가능
   - 프라이버시 제약

5. **성능 제어 불가**
   - Gemini 서버 성능에 의존
   - Rate Limit 제어 불가
   - 동시 세션 제한

---

## TO-BE: 원래 계획

### 아키텍처 (project-analysis_v2.md)

```
┌─────────────────────────────────────────┐
│   브라우저 (React - Netlify)             │
│  ┌────────────────────────────────────┐ │
│  │ • 마이크 입력 (Web Audio API)      │ │
│  │ • WebSocket 클라이언트             │ │
│  │ • 오디오 재생                      │ │
│  │ • UI/UX                            │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
            ↕ WebSocket (실시간 양방향)
┌─────────────────────────────────────────┐
│  백엔드 서버 (Python - Render.com)       │
│  ┌────────────────────────────────────┐ │
│  │ FastAPI + WebSocket                │ │
│  │ ┌────────────┐  ┌───────────────┐ │ │
│  │ │ Gemini API │  │ Rust Whisper  │ │ │
│  │ │ Integration│  │ STT Service   │ │ │
│  │ └────────────┘  └───────────────┘ │ │
│  │ • 오디오 스트림 처리               │ │
│  │ • 번역 로직                        │ │
│  │ • API 키 관리                      │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 기술 스택

#### 백엔드
```txt
# requirements.txt
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
google-generativeai>=0.3.0
pydantic>=2.5.0
python-dotenv>=1.0.0
pyo3>=0.20.0                    # ← Python-Rust 바인딩
numpy>=1.24.0                   # ← 오디오 처리
```

#### Rust STT 모듈
```toml
# rust_stt/Cargo.toml
[package]
name = "whisper-stt"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
whisper-rs = "0.10"             # ← Whisper C++ Rust 바인딩
tokio = { version = "1", features = ["full"] }

[lib]
name = "whisper_stt"
crate-type = ["cdylib"]
```

### 데이터 흐름 (Option A: Gemini Native Audio)

```
1. 사용자 음성 입력
   ↓
2. 브라우저: Web Audio API로 PCM 추출
   ↓
3. 브라우저 → 백엔드: WebSocket으로 오디오 청크 전송
   ↓
4. 백엔드: useWhisper 플래그 확인
   └─ Option A: Gemini Native Audio (현재 AS-IS와 동일)
```

### 데이터 흐름 (Option B: Whisper STT + Gemini Text)

```
1. 사용자 음성 입력
   ↓
2. 브라우저: Web Audio API로 PCM 추출
   ↓
3. 브라우저 → 백엔드: WebSocket으로 오디오 청크 전송
   ↓
4. 백엔드: useWhisper = true
   ├─ Step 1: Rust Whisper로 STT 처리
   │   └─ "안녕하세요" (텍스트 추출)
   │
   ├─ Step 2: Gemini Text API로 번역
   │   └─ "Hello" (번역 결과)
   │
   └─ Step 3: (선택) Gemini TTS로 음성 합성
       └─ PCM 오디오 생성
   ↓
5. 백엔드 → 브라우저: WebSocket으로 결과 전송
   ├─ 전사 텍스트: "안녕하세요"
   ├─ 번역 텍스트: "Hello"
   └─ 오디오 데이터: PCM
   ↓
6. 브라우저: 오디오 재생 + UI 업데이트
```

### 핵심 코드 (계획)

#### Rust Whisper 모듈 (rust_stt/src/lib.rs)
```rust
use pyo3::prelude::*;
use whisper_rs::{WhisperContext, FullParams};

#[pyclass]
struct WhisperModel {
    ctx: WhisperContext,
}

#[pymethods]
impl WhisperModel {
    #[new]
    fn new(model_path: String) -> PyResult<Self> {
        let ctx = WhisperContext::new_with_params(
            &model_path,
            WhisperContextParameters::default(),
        )?;
        Ok(WhisperModel { ctx })
    }

    fn transcribe(&mut self, audio_data: Vec<f32>) -> PyResult<PyObject> {
        let mut params = FullParams::new(SamplingStrategy::Greedy { best_of: 1 });
        params.set_language(Some("auto"));

        let mut state = self.ctx.create_state()?;
        state.full(params, &audio_data)?;

        let mut full_text = String::new();
        for i in 0..state.full_n_segments()? {
            full_text.push_str(&state.full_get_segment_text(i)?);
        }

        // Return {"text": full_text}
        Ok(result)
    }
}
```

#### WhisperService 클래스 (app/services/whisper_service.py)
```python
import whisper_stt  # Rust 모듈 import

class WhisperService:
    def __init__(self, model_path: str = "./rust_stt/models/ggml-base.bin"):
        self.model = whisper_stt.WhisperModel(model_path)

    async def transcribe(self, base64_audio: str) -> Optional[str]:
        # base64 → bytes → numpy array
        audio_bytes = base64.b64decode(base64_audio)
        audio_array = np.frombuffer(audio_bytes, dtype=np.int16).astype(np.float32) / 32768.0

        # Rust Whisper 호출
        result = self.model.transcribe(audio_array.tolist())
        return result.get("text", "")
```

#### WebSocketHandler 통합 (app/websocket/handler.py)
```python
async def _handle_init(self, message: InitMessage):
    self.use_whisper = message.config.useWhisper

    self.gemini_service = GeminiService(...)
    await self.gemini_service.connect()

    # Whisper 초기화
    if self.use_whisper:
        self.whisper_service = WhisperService()

async def _handle_audio(self, message: AudioMessage):
    if self.use_whisper and self.whisper_service:
        # Option B: Whisper STT → Gemini Text
        transcription = await self.whisper_service.transcribe(message.data)
        if transcription:
            await self.gemini_service.send_text(transcription)
    else:
        # Option A: Gemini Native Audio
        await self.gemini_service.send_audio(message.data)
```

### 주요 특징

| 특징 | 설명 |
|------|------|
| **이중 경로** | Gemini Native Audio / Whisper STT 선택 가능 |
| **Rust 통합** | PyO3로 Python-Rust 바인딩 |
| **모델 다운로드** | Whisper 모델 로컬 저장 (74MB~2.9GB) |
| **오프라인 가능** | Whisper로 로컬 STT 처리 |
| **커스터마이징** | STT 모델 선택, 파라미터 조정 가능 |

### 장점 ✅

1. **유연성**
   - 두 가지 STT 방식 선택 가능
   - 상황에 따라 최적 방식 사용
   - 기능 확장 용이

2. **성능 제어**
   - Whisper 모델 선택 (base/small/medium/large)
   - STT 파라미터 조정
   - 로컬 처리로 지연 시간 제어

3. **비용 절감 가능성**
   - STT 부분 무료 (Whisper)
   - Gemini API 호출 횟수 감소
   - 트래픽 예측 가능

4. **오프라인 지원**
   - 로컬 STT 처리
   - 네트워크 독립적
   - 프라이버시 강화

5. **벤더 독립성**
   - Gemini 장애 시 대체 가능
   - 멀티 벤더 전략
   - 리스크 분산

### 단점 ❌

1. **복잡성 증가**
   - Rust 빌드 파이프라인 필요
   - Python-Rust 바인딩 관리
   - 코드베이스 확대

2. **배포 어려움**
   - Rust 컴파일 환경 필요
   - 멀티 플랫폼 빌드 (Linux/macOS/Windows)
   - Render.com에서 Rust 지원 확인 필요

3. **개발 시간**
   - Rust 학습 곡선
   - PyO3 디버깅 어려움
   - 통합 테스트 복잡

4. **유지보수 부담**
   - 두 가지 언어 관리
   - 의존성 관리 복잡
   - 버전 호환성 이슈

5. **지연 시간 증가 (Option B)**
   - STT 처리 시간 추가
   - API 2회 호출 (Whisper → Gemini)
   - 네트워크 왕복 증가

6. **리소스 요구사항**
   - Whisper 모델 메모리 (500MB~3GB)
   - CPU 사용량 증가
   - 서버 스펙 상향 필요

---

## 비교 분석

### 기능 비교

| 기능 | AS-IS (현재) | TO-BE (계획) | 차이점 |
|------|--------------|--------------|--------|
| **STT** | ✅ Gemini | ✅ Gemini / Whisper | 선택 가능 여부 |
| **번역** | ✅ Gemini Live | ✅ Gemini (Text/Live) | API 방식 |
| **TTS** | ✅ Gemini | ✅ Gemini | 동일 |
| **전사** | ✅ 입력/출력 | ✅ 입력/출력 | 동일 |
| **WebSocket** | ✅ FastAPI | ✅ FastAPI | 동일 |
| **오프라인** | ❌ 불가능 | ✅ 가능 (Whisper) | 핵심 차이 |
| **커스터마이징** | ❌ 제한적 | ✅ 가능 | 핵심 차이 |

### 기술 스택 비교

| 구성요소 | AS-IS | TO-BE | 복잡도 |
|----------|-------|-------|--------|
| **언어** | Python | Python + Rust | ⬆️ 증가 |
| **의존성** | 8개 패키지 | 11개+ 패키지 | ⬆️ 증가 |
| **빌드** | pip install | pip + cargo build | ⬆️ 증가 |
| **배포** | 단순 (Python) | 복잡 (Multi-stage) | ⬆️ 증가 |
| **모델** | 없음 | Whisper (74MB~3GB) | ⬆️ 증가 |

### 성능 비교

| 지표 | AS-IS | TO-BE (Option A) | TO-BE (Option B) |
|------|-------|------------------|------------------|
| **API 호출** | 1회 (Live) | 1회 (Live) | 2회 (Whisper+Text) |
| **지연 시간** | ~300ms | ~300ms | ~500ms |
| **네트워크** | 중간 | 중간 | 높음 |
| **CPU** | 낮음 | 낮음 | 높음 (Whisper) |
| **메모리** | ~100MB | ~100MB | ~500MB~3GB |

### 비용 비교

| 항목 | AS-IS | TO-BE |
|------|-------|-------|
| **개발 시간** | 2주 | 4~6주 |
| **학습 곡선** | 낮음 | 높음 (Rust) |
| **서버 비용** | $7/월 (Starter) | $25/월 (Standard, Whisper용) |
| **Gemini API** | 무료 → 유료 | 무료 → 유료 (호출 횟수 감소) |
| **유지보수** | 낮음 | 높음 |

### 장단점 종합

#### AS-IS 선택 시나리오

✅ **적합한 경우:**
- 빠른 출시 필요
- 소규모 사용자 (무료 할당량 내)
- 단순한 유지보수 선호
- Python 전문 팀
- 클라우드 환경

❌ **부적합한 경우:**
- 오프라인 필요
- 대규모 트래픽 (API 비용 증가)
- 벤더 독립성 중요
- STT 커스터마이징 필요

#### TO-BE 선택 시나리오

✅ **적합한 경우:**
- 오프라인 지원 필수
- 대규모 트래픽 (API 비용 절감)
- STT 성능 제어 필요
- 멀티 벤더 전략
- Rust 전문성 보유

❌ **부적합한 경우:**
- 빠른 출시 필요
- 소규모 팀
- Python만 사용
- 단순한 아키텍처 선호

---

## 의사결정 배경

### 왜 AS-IS를 선택했는가?

#### 1. **Gemini Native Audio API의 완전성**

```python
# Gemini Live API가 제공하는 모든 기능
config = {
    "input_audio_transcription": {},      # ① STT
    "system_instruction": "번역 지시",    # ② LLM 번역
    "response_modalities": ["AUDIO"],     # ③ TTS
    "output_audio_transcription": {},     # ④ 전사
}
```

**판단**: Whisper 추가 없이 완전한 기능 제공

#### 2. **실용주의적 접근**

| 요소 | 판단 |
|------|------|
| **MVP 속도** | 2주 vs 6주 → AS-IS 선택 |
| **팀 역량** | Python 전문 vs Rust 학습 필요 → AS-IS 선택 |
| **배포 환경** | Render.com Python 간편 vs Rust 복잡 → AS-IS 선택 |
| **초기 트래픽** | 무료 할당량 충분 → AS-IS 선택 |

#### 3. **기술 부채 회피**

```
TO-BE 선택 시 부채:
- Rust 빌드 파이프라인 유지보수
- Python-Rust 바인딩 버전 관리
- Whisper 모델 업데이트 관리
- 멀티 플랫폼 빌드 이슈
```

**판단**: 초기 단계에서 불필요한 복잡성 회피

#### 4. **점진적 개선 가능성**

```
Phase 1: AS-IS 구현 (완료) ✅
  → 빠른 검증, 사용자 피드백

Phase 2: TO-BE 추가 (미래)
  └─ 필요 시 Whisper 옵션 추가
     - 사용량 증가 시
     - 오프라인 요구 시
     - 비용 문제 발생 시
```

**판단**: 필요할 때 추가해도 늦지 않음

### 의사결정 프로세스

```
1. 요구사항 분석
   ├─ 필수: API 키 보안, 실시간 통신
   └─ 선택: 오프라인, 커스터마이징

2. 옵션 평가
   ├─ Option 1: Gemini Only (AS-IS)
   └─ Option 2: Gemini + Whisper (TO-BE)

3. 평가 기준
   ├─ 개발 속도: AS-IS 승리 ✅
   ├─ 배포 복잡도: AS-IS 승리 ✅
   ├─ 유지보수: AS-IS 승리 ✅
   ├─ 초기 비용: 동일 (무료 할당량)
   └─ 기능 완전성: 동일 (Gemini 충분)

4. 최종 결정: AS-IS 선택
   └─ 이유: 빠른 검증 + 낮은 복잡도 + 향후 확장 가능
```

---

## 향후 고려사항

### Phase 2: Whisper 통합 검토 (조건부)

**트리거 조건:**
1. **비용 문제**
   - Gemini API 비용 > $100/월
   - 트래픽 급증 예상

2. **기능 요구사항**
   - 오프라인 지원 필수
   - STT 커스터마이징 필요
   - 특정 언어 성능 개선 필요

3. **벤더 리스크**
   - Gemini API 장애 빈번
   - 정책 변경 위험
   - 대체 솔루션 필요

### 하이브리드 전략

```
┌─────────────────────────────────────────┐
│  백엔드 서버                             │
│  ┌────────────────────────────────────┐ │
│  │ Router                             │ │
│  │  ├─ 기본: Gemini Native Audio     │ │
│  │  ├─ 고트래픽: Whisper STT          │ │
│  │  └─ 오프라인: Whisper + Local LLM │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

**구현 전략:**
1. **단계적 전환**
   - Phase 1: 10% 트래픽 Whisper 테스트
   - Phase 2: 비용/성능 비교
   - Phase 3: 최적 비율 결정

2. **Feature Flag**
   ```python
   if user.is_premium or traffic > threshold:
       use_whisper = True
   else:
       use_whisper = False
   ```

3. **비용 최적화**
   ```
   저트래픽 사용자: Gemini (간편)
   고트래픽 사용자: Whisper (비용 절감)
   프리미엄 사용자: 선택 가능
   ```

### 대체 기술 검토

| 기술 | 장점 | 단점 | 적용 시점 |
|------|------|------|-----------|
| **Google STT API** | 고품질, 다국어 | 비용 | 비용 문제 시 |
| **Azure Speech** | 안정적, 엔터프라이즈 | 비용 | 기업 고객 |
| **Faster-Whisper** | 빠름 (4x) | Rust 대신 Python | Whisper 성능 이슈 시 |
| **WebAssembly Whisper** | 클라이언트 처리 | 브라우저 부담 | 극단적 비용 절감 |

### 모니터링 지표

**Phase 2 결정을 위한 KPI:**
1. **비용**
   - Gemini API 월 비용
   - 사용자당 평균 비용
   - 비용 증가율

2. **성능**
   - 평균 응답 시간
   - 에러율
   - 동시 세션 수

3. **사용자**
   - 일일 활성 사용자 (DAU)
   - 세션당 평균 턴 수
   - 사용자 만족도

**임계값:**
- 월 비용 > $100 → Whisper 검토
- 평균 지연 > 500ms → 최적화 필요
- 에러율 > 5% → 대체 방안 검토

---

## 결론

### AS-IS 선택의 정당성

1. ✅ **Gemini Native Audio API는 완전한 솔루션**
   - STT + 번역 + TTS + 전사 모두 제공
   - 추가 구현 불필요

2. ✅ **빠른 시장 진입**
   - 2주 개발 → 빠른 사용자 피드백
   - MVP 검증 완료

3. ✅ **낮은 기술 부채**
   - Python 단일 스택
   - 단순한 아키텍처
   - 쉬운 유지보수

4. ✅ **점진적 개선 가능**
   - 필요 시 Whisper 추가
   - 하이브리드 전략 가능
   - 옵션 보존

### TO-BE는 미래의 옵션

```
현재 상태:
  AS-IS (Gemini Only) ✅ 운영 중

미래 옵션:
  TO-BE (+ Whisper) ⏳ 필요 시 추가

  트리거:
    ├─ 비용 > $100/월
    ├─ 오프라인 필요
    └─ 대규모 트래픽
```

**project-analysis_v2.md는 "이상적 설계 문서"이며, AS-IS는 "실용적 구현"입니다.**

---

## 참고 자료

- [project-analysis_v2.md](./project-analysis_v2.md) - 원래 계획 문서
- [Gemini Live API Documentation](https://ai.google.dev/api/live-connect)
- [Whisper.cpp GitHub](https://github.com/ggerganov/whisper.cpp)
- [FastAPI WebSocket Guide](https://fastapi.tiangolo.com/advanced/websockets/)
- [Render.com Rust Deployment](https://render.com/docs/deploy-rust)

---

**문서 버전**: 1.0
**작성일**: 2024-12-11
**작성자**: AI Assistant
**목적**: 현재 구현과 원래 계획 비교 분석
